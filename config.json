{
    "name": "vuln-finder",
    "version": "v0.0.18-alpha",
    "image_name": "codeclarityce/plugin-vuln-finder",
    "depends_on": [
        "js-sbom",
        "php-sbom"
    ],
    "description": "A plugin to find vulnerabilities in JavaScript and PHP projects",
    "config": {
        "vulnerabilityPolicy": {
            "name": "Vulnerability Policy",
            "type": "Array<string>",
            "required": false,
            "description": "A list of vulnerabilities that are blacklisted in the project"
        }
    }
}